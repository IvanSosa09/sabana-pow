import { Measure } from './parseMeasure';
interface AnimationOptions {
    object: {
        [prop: string]: Measure;
    };
    prop: string;
    target: Measure;
    duration?: number;
    speed?: number;
    acceleration?: number;
    maxSpeed?: number;
    onUpdate?: (value: string) => void;
    onComplete?: () => void;
}
export declare function animate({ object, prop, target, duration, speed, acceleration, maxSpeed, onUpdate, onComplete, }: AnimationOptions): () => void;
export {};
